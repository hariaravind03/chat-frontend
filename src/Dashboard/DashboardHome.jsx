import React from 'react';
import { Typography } from '@mui/material';

const DashboardHome = () => {
  return (
    <Typography>
      Choose an option above to manage your friends.
    </Typography>
  );
};

export default DashboardHome;
